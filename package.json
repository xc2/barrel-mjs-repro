{
  "type": "module",
  "scripts": {
    "build": "bun run --bun build:rslib && bun run --bun build:rspack",
    "build:rspack": "rspack build",
    "build:rslib": "rslib build"
  },
  "packageManager": "bun@1.1.34",
  "dependencies": {
    "@rslib/core": "^0.0.16",
    "@rspack/core": "1.0.14",
    "@rspack/cli": "1.0.14",
    "@types/bun": "^1.1.13"
  }
}
